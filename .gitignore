notes.md
CLAUDE.md

# Local environment files — derived from .env.example, never committed
.env

# Python build artifacts (regenerated by pip/setuptools — never commit)
__pycache__/
*.py[cod]
build/
dist/
*.egg-info/
.venv/
venv/

# Personal learning notes — kept locally, not part of the project deliverable
learning-docs/
