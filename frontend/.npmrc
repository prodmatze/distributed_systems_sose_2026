# Node version is a warning, not a gate. next@16 requires >=20.9.0 and that
# is what package.json asks for; a stricter setting here previously refused
# to run ANY pnpm script, which blocked `make demo` on a fresh clone.
engine-strict=false
