# Copy this file to `.env` in the same directory and fill in real values for local dev.
# The `.env` file is gitignored — never commit credentials.

# --- Postgres ---
POSTGRES_USER=chorus
POSTGRES_PASSWORD=chorus_dev_password
POSTGRES_DB=chorus
